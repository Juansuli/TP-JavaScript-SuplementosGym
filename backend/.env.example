# Copiar este archivo a ".env" (sin el ".example") y completar con los
# datos reales. El archivo ".env" nunca se sube al repositorio.

PORT=3001

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=suplementos_gym
